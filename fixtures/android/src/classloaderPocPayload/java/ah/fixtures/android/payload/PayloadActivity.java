package ah.fixtures.android.payload;

import android.app.Activity;

/** Activity class intentionally compiled only into the in-memory PoC payload. */
public final class PayloadActivity extends Activity {}
