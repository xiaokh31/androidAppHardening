# M2-02 local validation

## 2026-08-07 bounded format foundation

- Branch/commit before implementation: `feat/m2-02-native-decrypt-loader@311913e857062539bc4e14acb1e0dcff0164e968`.
- Scope: dependency-free C++17 parsing for exact 160-byte HeaderV2, bounded SPV1, 128-byte RecordV2, 32-byte ChunkV2, 768-byte ConfigV2 and cross-record/chunk topology. No JNI, ClassLoader, APK write, device or emulator operation was performed.
- Fail-closed bounds include 64 DEX, 16 lineage entries, 65,536 chunks, 64 KiB canonical chunks, 512 MiB per DEX, 4 GiB total DEX, checked payload/tag arithmetic, canonical DEX names, nonzero nonce prefixes, reserved/zero-fill fields and the shell-Factory exclusion.
- MSVC command: compile `container_format.cpp` and `container_format_test.cpp` with Visual Studio 2022 x64 `cl.exe /std:c++17 /EHsc /W4 /WX /DAH_CONTAINER_FORMAT_STANDALONE_TEST`, then run the generated ignored test executable. Exit code `0` at `2026-08-07T14:03:42+08:00`.
- Android command: repo-local JDK 17 and Gradle user home, `gradlew.bat :runtime:native:assembleRelease --offline --no-daemon --no-configuration-cache`. Exit code `0` in 30 seconds at `2026-08-07T14:04:35+08:00`; `armeabi-v7a`, `arm64-v8a`, `x86` and `x86_64` CMake builds all succeeded.
- Generated `.obj`, executable, AAR and native build outputs remain untracked under ignored build locations; two accidentally emitted root `.obj` files were immediately removed after exact absolute-path validation.

The foundation does not yet claim M2-02 completion. Native ZIP asset location, Config/share recovery, manifest HMAC verification, per-chunk GCM-to-zlib transaction ownership, anonymous mappings, JNI/Java facade and device matrices remain required before freeze or publication.

## 2026-08-07 authenticated Native transaction checkpoint

- Implemented a bounded ZIP32 locator for the only two accepted names. It rejects ZIP64/multi-disk/sentinel fields, ambiguous EOCD, duplicate or missing fixed assets, encryption, data descriptors, non-STORED methods, local/central mismatches, non-4-KiB data offsets, overlapping ranges, size violations and CRC-32 mismatches. It returns read-only byte ranges and performs no extraction or temporary-file write.
- Added exact 104-byte `NativeShareSlotV1` parsing and the ordered binding chain: slot SHA-256/ABI/build/key-slot, measured signer and Framework package digest, ADR 0006 KEK, ConfigV2 CEK envelope, streaming ADR 0008 manifest HMAC, authenticated config digest, and final signer/version/build/key-slot cross-checks. SHA-256 and segmented HMAC use the same serialized pinned PSA backend as AES-GCM/HKDF; no second crypto implementation or raw remote dependency was added.
- Added per-record key derivation and a fixed 65,536-byte authenticated-compressed buffer. Each chunk uses one-shot AES-256-GCM and is submitted to the record's single continuous zlib-wrapped inflater only after tag success. The loader separately classifies wrapper, dictionary, checksum, length, digest and trailing-stream failures, enforces the authenticated original length, and hashes the completed anonymous mapping.
- Added fixed-capacity transaction ownership for at most 64 anonymous mappings. Before commit, the transaction alone owns completed and partial DEX mappings; rollback clears and unmaps every mapping without allocating, continues after cleanup errors and reports cleanup failure separately without replacing the primary status. Commit atomically transfers all mappings to `PayloadHandle`, whose close path uses the same clear/unmap routine.
- `node tools/validation/prepare-m2-02-native-vector.mjs` exited `0` and prepared ignored ConfigV2/AHDC/slot inputs from the existing byte-identical M1-04 vector after verifying its container SHA-256 and config bindings. The Linux Host test consumes this vector and includes positive two-DEX open plus CEK tag, slot, manifest and first/middle/last chunk-tag rollback cases; the Ubuntu workflow now prepares and runs it before the root matrix.
- Visual Studio 2022 x64 strict syntax command compiled `container_format.cpp`, `zip_assets.cpp`, `payload_memory.cpp` and `m2_02_foundation_test.cpp` with `/std:c++17 /EHsc /W4 /WX /c`; exit code `0`. A full local MSVC link remains inapplicable for the already documented upstream TF-PSA zero-array limitation; the pinned Windows CI uses its reviewed `clang-cl` path.
- Repo-local JDK 17/Gradle command `:runtime:native:assembleRelease --offline --no-daemon` exited `0` in 29 seconds with `-Wall -Wextra -Wpedantic -Werror`; all four Android ABIs built. `llvm-nm` found the retained authenticated-open/ZIP/transaction symbols in every unstripped ABI output, proving link GC did not discard the implementation.
- Repo-local JDK 17/Gradle command `check --offline --no-daemon` exited `0` in 84 seconds (`249` actionable tasks). The M1-04 13-case container matrix, M1-01/02/03/05/06 regressions, lint and toolchain policy all passed. No device, emulator, APK installation, network download or C-drive tool download was used.

This is an implementation checkpoint, not M2-02 completion. Read-only `sourceDir` file mapping, JNI primitive-handle facade, authenticated metadata transfer, `InMemoryDexClassLoader`, Java post-handle rollback windows, sanitizer/fuzz/OOM injection and device/KVM acceptance remain before freeze, independent review or publication.

## 2026-08-08 JNI and Java ownership checkpoint

- Added a descriptor-backed `ApplicationInfo.sourceDir` reader that accepts only absolute paths, opens with `O_RDONLY | O_CLOEXEC | O_NOFOLLOW`, requires one regular file no larger than ZIP32, and keeps one `PROT_READ | MAP_PRIVATE` mapping alive across fixed-asset location and the entire authenticated open. It never extracts an asset or writes a DEX file.
- Completed DEX mappings are switched from read/write to OS-level read-only before transaction commit. Close restores write permission only to clear plaintext, then unmaps every mapping; partial read-only commit failures roll back through the same non-allocating path.
- Added a fixed 32-slot typed-handle registry. The Java `long` encodes a positive generation and slot index rather than a C++ pointer; every metadata/buffer lookup validates both under one mutex, and `nativeClosePayload` removes ownership before its allocation-free clear/unmap operation.
- Added the fixed internal `AHMD` metadata encoding and strict Java parser for container `2.0`, signer/risk policy `1/1`, nullable strict Factory FQCN, 16-byte build/key IDs, 32-byte package/current-signer digests, and `1..16` old-to-new signer lineage entries. `AuthenticatedPayloadMetadata` exposes only defensive copies and contains no share, CEK/KEK, nonce, tag, wrapped CEK or raw ConfigV2 bytes. The separate `AHUB` object and its accessor documentation remain explicitly unauthenticated.
- Added the exact five package-private JNI bridge methods and `System.loadLibrary("ah_runtime")`. A narrowly scoped consumer rule preserves only the frozen JNI bridge class/method names under R8. The only public low-level entry is `PayloadRuntime`; public API inspection with `javap` shows only the two fixed facade methods, three `LoadedPayload` methods, ten authenticated metadata getters and three untrusted pre-read getters.
- `PayloadRuntime.openVerified` keeps the returned Native handle in a primitive local with `committed=false`. It constructs metadata bytes/object, direct read-only buffers, the M0-05-equivalent bounded public-API native search path, the three-argument API 29 `InMemoryDexClassLoader`, and finally `LoadedPayload`, in that order. Any pre-commit `RuntimeException` or `Error` closes the Native handle exactly once in `finally`, clears partial strong references and preserves the primary error; no payload lookup, Factory construction, callback registration or bootstrap publication is performed.
- Repo-local JDK 17 Gradle `:runtime:native:compileReleaseJavaWithJavac :runtime:native:lintRelease` exited `0` in 32 seconds. Repo-local Gradle/NDK `:runtime:native:externalNativeBuildRelease` exited `0` in 25 seconds and built `armeabi-v7a`, `arm64-v8a`, `x86` and `x86_64` with `-Wall -Wextra -Wpedantic -Werror`.
- Final offline repo-local JDK 17/Gradle `:runtime:native:assembleRelease check` exited `0` in 94 seconds (`284` actionable tasks); the Release AAR, all four ABI libraries, Java/lint, M1-01/02/03/04/05/06 regressions, toolchain policy and root checks passed without a network request.
- NDK 29 `llvm-readelf` found exactly one 104-byte alloc/read-only `.ah_share_v1` section in each ABI output; the x86_64 dynamic symbol table contains the exact five fixed JNI entries. No local emulator, device, APK installation, network download or new C-drive artifact was used.
- The rebuilt ignored `native-release.aar` is `431220` bytes with SHA-256 `1f14654e26e9ce9c9a643af79a49e623bcf42ebd085380f03a2a4ef719afd07f`; it contains exactly one `libah_runtime.so` under each of the four ABI directories, and its `proguard.txt` contains the narrow `NativePayloadBridge` keep rule.
- A local Windows host link attempt remains inapplicable because the pinned minimal TF-PSA configuration produces its already documented disabled-PK zero-array error under MSVC. This does not affect the four-ABI Clang production build; the Linux Host vector, sanitizer/fuzz/OOM-injection and device/KVM matrices remain mandatory before task freeze.

This is still an implementation checkpoint, not M2-02 completion or publication. The next bounded layer is failure-injection/host sanitizer coverage and the authorized API 29/36 KVM plus arm64 device acceptance; M2-03 remains prohibited.
