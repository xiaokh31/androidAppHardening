# Security Policy

## 支持状态

仓库当前处于 M0/MVP 开发阶段，尚无生产就绪版本。正式支持范围以最新发布说明和 `docs/COMPATIBILITY_MATRIX.md` 为准。

## 报告漏洞

请使用 GitHub 仓库的私有 Security Advisory 提交漏洞，不要在公开 Issue 中披露可直接利用的细节、真实 APK、证书、密钥、用户路径或运行环境凭据。

报告应包括：

- 受影响提交或版本。
- 最小合成复现步骤。
- 预期行为与实际行为。
- 安全影响和所需攻击前提。
- 已执行的验证命令与环境。

维护者确认前，不要发布利用代码、反编译客户内容或绕过细节。

## 安全边界

- 本项目只提高 DEX 静态提取、二次打包、普通内存截取、调试和 Hook 的成本。
- 拥有 root、Frida、定制 ART、内核或可修改壳代码能力的攻击者仍可能取得运行时明文或绕过客户端检查。
- 离线 Runtime 必须包含可恢复的密钥材料；项目不会将其描述为不可提取。
- 签名匹配、AEAD 和关键文件完整性失败属于强制阻断；环境风险信号允许按照策略处理误报。

## 敏感材料

仓库、Issue、PR、CI 和 Agent 会话中不得出现：

- 生产 keystore、私钥、密码、alias 或访问令牌。
- 客户 APK、客户文件路径、明文 DEX 或反编译源码。
- 未脱敏的设备标识、账户资料或内部基础设施地址。

如敏感材料被误提交，立即停止传播，撤销相关凭据，并通过私有 Security Advisory 协调历史清理。
