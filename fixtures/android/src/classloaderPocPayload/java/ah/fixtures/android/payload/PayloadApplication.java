package ah.fixtures.android.payload;

import android.app.Application;

/** Application class intentionally compiled only into the in-memory PoC payload. */
public final class PayloadApplication extends Application {}
